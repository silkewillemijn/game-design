using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlaySound2 : MonoBehaviour
{

    public AudioSource voice2a;
    GameObject posterpiece1;

    // Start is called before the first frame update
    void Start()
    {
        posterpiece1 = GameObject.Find("Poster 1");
    }

    // Update is called once per frame
    void Update()
    {
        // if (ObjectsToCollect.
        //     Debug.Log("Poster 1 dissapeared");
        // }
    }
}
